const toErrorCheck = error => ({ type: error.type, message: error.message });

export const expectError = (error, expectedErrors) => {
  let errors = error.map ? error : [error];
  errors = errors.map(toErrorCheck);
  expectedErrors = expectedErrors.map ? expectedErrors : [expectedErrors];
  expect(errors).toMatchObject(expectedErrors);
};
